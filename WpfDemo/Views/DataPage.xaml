<Page x:Class="WpfDemo.Views.DataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:models="clr-namespace:WpfDemo.Models"
      Background="#0F172A" Title="Data Grid">

    <Page.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background"   Value="#1E293B"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding"      Value="0"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"    Value="#0F172A"/>
            <Setter Property="Foreground"    Value="#64748B"/>
            <Setter Property="FontSize"      Value="11"/>
            <Setter Property="FontWeight"    Value="SemiBold"/>
            <Setter Property="Padding"       Value="12,10"/>
            <Setter Property="BorderBrush"   Value="#334155"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CBD5E1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E293B"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#312E81"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="12,10"/>
            <Setter Property="Foreground"      Value="#CBD5E1"/>
        </Style>
    </Page.Resources>

    <Grid Margin="32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Data Grid" FontSize="28" FontWeight="Bold" Foreground="#F1F5F9"/>
            <TextBlock Text="Sortable, searchable data table with live filtering"
                       FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Search -->
        <Border Grid.Row="1" Background="#1E293B" CornerRadius="12"
                Padding="14,10" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍" FontSize="16"
                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Background="Transparent" Foreground="#F1F5F9"
                         BorderThickness="0" FontSize="14"
                         VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding FilteredPeople}"
                      SelectedItem="{Binding SelectedPerson}"
                      AutoGenerateColumns="False"
                      CanUserSortColumns="True"
                      CanUserResizeColumns="True"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#1F2937"
                      Background="Transparent"
                      BorderThickness="0"
                      RowHeight="42"
                      ColumnHeaderHeight="40"
                      SelectionMode="Single">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="NAME"       Binding="{Binding Name}"       Width="*"/>
                    <DataGridTextColumn Header="AGE"        Binding="{Binding Age}"        Width="60"/>
                    <DataGridTextColumn Header="DEPARTMENT" Binding="{Binding Department}" Width="140"/>
                    <DataGridTemplateColumn Header="STATUS" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate DataType="{x:Type models:PersonModel}">
                                <Border Background="#334155" CornerRadius="20"
                                        Padding="10,3" Margin="4,6" HorizontalAlignment="Left"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Status}" FontSize="12" Foreground="#CBD5E1"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="SCORE" Width="80">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Score" StringFormat="{}{0:F1}"/>
                        </DataGridTextColumn.Binding>
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#34D399"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Detail Bar -->
        <Border Grid.Row="3" Background="#1E293B" CornerRadius="12"
                Padding="16,12" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal">
                <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                    <TextBlock Text="Selected:" FontSize="13" Foreground="#64748B" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding SelectedPerson.Name, FallbackValue='—'}"
                               FontSize="13" FontWeight="SemiBold" Foreground="#A78BFA" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                    <TextBlock Text="Dept:" FontSize="13" Foreground="#64748B" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding SelectedPerson.Department, FallbackValue='—'}"
                               FontSize="13" Foreground="#CBD5E1" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Score:" FontSize="13" Foreground="#64748B" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock FontSize="13" Foreground="#34D399" FontWeight="Bold" VerticalAlignment="Center">
                        <TextBlock.Text>
                            <Binding Path="SelectedPerson.Score" StringFormat="{}{0:F1}" FallbackValue="—"/>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>

    </Grid>
</Page>
