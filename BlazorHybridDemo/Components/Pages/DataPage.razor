@page "/data"
@inject AppService Svc

<h1 style="font-size:24px;font-weight:700;margin-bottom:8px;">📊 Data Grid</h1>
<p style="font-size:14px;color:#6B7280;margin-bottom:28px;">資料直接從 C# 服務載入</p>

<div style="background:#1E293B;border:1px solid #334155;border-radius:12px;overflow:hidden;">
    <table style="width:100%;border-collapse:collapse;font-size:13px;">
        <thead style="background:#0F172A;">
            <tr>
                @foreach (var h in new[]{"姓名","職位","部門","薪資","狀態"})
                {
                    <th style="padding:12px 16px;text-align:left;color:#6B7280;font-size:11px;font-weight:600;text-transform:uppercase;border-bottom:1px solid #334155;">@h</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var r in _rows)
            {
                <tr style="border-bottom:1px solid #1E293B;">
                    <td style="padding:12px 16px;color:#F9FAFB;font-weight:500;">@r.Name</td>
                    <td style="padding:12px 16px;color:#D1D5DB;">@r.Role</td>
                    <td style="padding:12px 16px;color:#D1D5DB;">@r.Dept</td>
                    <td style="padding:12px 16px;color:#6EE7B7;">$@r.Salary.ToString("N0")</td>
                    <td style="padding:12px 16px;">
                        <span style="display:inline-block;padding:3px 10px;border-radius:999px;font-size:11px;font-weight:600;background:@(r.Status=="Active"?"#064E3B":"#1E3A5F");color:@(r.Status=="Active"?"#6EE7B7":"#93C5FD");">@r.Status</span>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@code {
    List<AppService.DataRow> _rows = new();
    protected override void OnInitialized() => _rows = Svc.GetTableData();
}
