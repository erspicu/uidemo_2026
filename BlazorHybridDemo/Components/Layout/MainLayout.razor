@inherits LayoutComponentBase
@inject NavigationManager Nav

<div style="display:flex;height:100vh;background:#0F172A;font-family:'Segoe UI',system-ui,sans-serif;color:#F9FAFB;">
    <!-- Sidebar -->
    <div style="width:220px;background:#111827;border-right:1px solid #1F2937;display:flex;flex-direction:column;flex-shrink:0;">
        <div style="padding:22px 20px 18px;border-bottom:1px solid #1F2937;">
            <div style="font-size:17px;font-weight:700;color:#A78BFA;">🔷 Blazor Hybrid</div>
            <div style="font-size:11px;color:#6B7280;margin-top:4px;">.NET 8 · WPF + Blazor</div>
        </div>
        <div style="padding:14px 0 0;">
            <div style="font-size:10px;font-weight:600;color:#4B5563;padding:0 20px 8px;text-transform:uppercase;letter-spacing:.05em;">NAVIGATION</div>
            <NavButton Href="/" Icon="🏠" Label="Dashboard" />
            <NavButton Href="/controls" Icon="🎛️" Label="Controls" />
            <NavButton Href="/animation" Icon="✨" Label="Animation" />
            <NavButton Href="/data" Icon="📊" Label="Data Grid" />
        </div>
        <div style="margin-top:auto;padding:14px 20px;border-top:1px solid #1F2937;font-size:11px;color:#374151;">v1.0.0 · Demo App</div>
    </div>
    <!-- Content -->
    <div style="flex:1;overflow-y:auto;padding:32px;background:#0F172A;">
        @Body
    </div>
</div>

@code {
    private string CurrentPage => Nav.Uri.Contains("/controls") ? "controls"
        : Nav.Uri.Contains("/animation") ? "animation"
        : Nav.Uri.Contains("/data") ? "data" : "dashboard";
}
